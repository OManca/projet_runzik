// pour afficher au dessus de l'icône
// le nombre d'objets contenus dans le panier
function countItems(i) {
  let selection = document.body.querySelector('.panier_noir');
  selection.textContent='${i}';
  
}

// selectionner un produit pour l'ajouter 
// dans un tableau de produits sélectionnés
let selection = document.body.querySelector();